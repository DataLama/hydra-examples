db:
  driver: postgresql
  user: postgres_user
  pass: drowssap
  timeout: 10
ui:
  windows:
    create_db: true
    view: true
schema:
  database: support
  tables:
  - name: users
    fields:
    - name: string
    - password: string
  - name: issues
    fields:
    - title: string
    - description: string
    - reported_by: string
    - priority: int
